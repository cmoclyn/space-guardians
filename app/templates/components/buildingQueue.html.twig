<div {{ stimulus_controller('building-queue') }}>
    <small {{ stimulus_target('building-queue', 'name') }}></small>
    <small {{ stimulus_target('building-queue', 'remainingTime') }}></small>
    <div class="w-full h-2 bg-gray-800 rounded mt-2">
        <div
                {{ stimulus_target('building-queue', 'progressBar') }}
                class="h-full bg-blue-500 rounded transition-all duration-500 ease-out"
                style="width: 0%;"
        ></div>
    </div>
</div>